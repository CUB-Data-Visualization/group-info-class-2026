---
title: GitHub & Markdown Exercise
format: html
embed-resources: true
editor: source
---

# Name: Ethan Edwards 

# 1. Coding Experience

-   MATLAB (intermediate-advanced)

-   C (intermediate)

# 2. Semester Goals 

1.  Improve best coding practices so code can run flawlessly and also be referenced by a future engineer/scientist in the future.

2.  Expand software programming knowledge base as to understand what the computer is actually doing with each command. Different coding languages have different syntax but knowing how the program actually runs code will allow you to learn any language. 

3.  Learn more about data visualization and the workflow. I am not the strongest programmer in the world and it will be nice to learn how to organize my thoughts and changes.

# 3. Favorite Equation

@eq-fav is the inductor voltage equation which states that the voltage across an inductor is proportional to the rate of change of the current passing through it.

$$
V_{\rm L} = L \cdot \frac{dI_{\rm L}}{dt}
$${#eq-fav}

{{< pagebreak >}}

# 4. Flow Chart Practice

> Draw a little flowchart to practice this charting feature. It does not have to be complicated. Have fun with it. Don't forget to fill in a figure caption under `fig-cap`. 

@fig-chart shows my favorite flow chart.

```{mermaid}
%%| label: fig-chart
%%| fig-cap: Chart to see if you need water today.
graph LR
    A{Are you dehydrated?} -->|No| B(Are you sure?)
    A --> |Yes| C[Drink Water]
    B --> |No| C[Drink Water]
    B --> |Yes| D(You need water anyways)
    D --> C
```

::: callout-tip
Click on the "Preview" button above the code block to render your chart or use the [Mermaid Live Editor](https://mermaid.live/edit) if you want to see the output immediately.
:::

# 5. Something fun

> Add a picture of yourself doing something you enjoy outside school (hiking, cooking, playing music, reading, etc) to the `images` folder and link to it here with a descriptive caption by changing the `fix-cap` below. Make sure that images are not too big and in **jpeg/jpg** or **png** format. A width of 600 to maximally 1200 pixels is typically plenty large (i.e. less than **\~2Mb**). You can scale your image to these dimensions using any photo editing software. For example, a convenient free online tool for scaling images is available at <http://www.simpleimageresizer.com/>. Lastly, the file names are case-sensitive! So `example.PNG` (upper-case PNG) is not the same as `example.png` (lower-case png), make sure that you have the filename correctly.

@fig-fun shows how much fun I'm having.

```{r}
#| label: fig-fun
#| fig-cap: My kids love colorful dinosaurs.
#| echo: FALSE
#| out-width: 100%
knitr::include_graphics("images/example.png")
```
